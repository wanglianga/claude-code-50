# compose 项目名（由评估环境固定，勿改）
COMPOSE_PROJECT_NAME=task2026-09-1250

# 应用服务发布到宿主机的端口（宿主:容器8080）
CC_PUBLISH_PORT=3050

# PostgreSQL 配置（仅 compose 内网访问，不发布宿主端口）
DB_NAME=nightpharmacy
DB_USER=night
DB_PASSWORD=nightpass
